server:
  port: 8082

spring:
  application:
    name: feirantes-service
  datasource:
    url: jdbc:postgresql://localhost:5433/feirantes_db
    username: user_feirantes
    password: password123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect