server:
  port: 8083

spring:
  application:
    name: cidadaos-service
  datasource:
    url: jdbc:postgresql://localhost:5434/cidadaos_db
    username: user_cidadaos
    password: password123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect