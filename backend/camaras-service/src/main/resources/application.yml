server:
  port: 8081

spring:
  application:
    name: camaras-service
  datasource:
    url: jdbc:postgresql://localhost:5432/camaras_db
    username: user_camaras
    password: password123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect